# 📋 TODO 리스트

## 🚀 진행 및 학습 예정 항목
1. **StockPage 컴포넌트 컨테이너화 하기**
    - 관심주식 목록 구현하기, 최근 검색목록 리스팅하기, 데이터 폴링 구현하기, STOCK 검색인풋 Header에 고정
2. **UI 라이브러리 도입 검토**: Tailwind를 보완할 Shadcn/UI 또는 MUI 적용하기
3. **주석 내용 복습 및 이해하기**

---

### 🌱 기본기 복습 (Foundations)
- **13. JavaScript 현대적 문법 및 배열 메서드 심화**
    - **학습 내용**: `Map`, `Set`, 그리고 `Array.reduce()`, `Array.filter()`, `Array.some/every()` 등의 고차 함수 완벽 숙달.
    - **추천 이유**: 주식 데이터는 대부분 배열 형태입니다. 데이터를 원하는 대로 자유자재로 가공(Formatting)할 수 있는 능력은 모든 프론트엔드 실무의 가장 강력한 기초 체력이 됩니다.
- **14. 브라우저 렌더링 원리 및 React 리렌더링 조건**
    - **학습 내용**: CRP(Critical Rendering Path) 단계와 React가 리렌더링을 결정하는 기준(State, Props, Context 변화).
    - **추천 이유**: "왜 이 컴포넌트가 다시 그려지지?" 혹은 "왜 이렇게 느리지?"라는 질문에 답할 수 있는 핵심 지식입니다. 리액트의 작동 원리를 알면 `useMemo`나 `useCallback`을 '진짜로 필요한 때'에만 쓸 수 있게 됩니다.

---

### 💡 시니어 추천 학습 항목 (심화 포함)

- **4. Zustand Persist (상태 영속성 관리)**
    - **학습 내용**: `zustand/middleware`의 `persist` 사용법 및 저장소(Storage) 전략 이해.
    - **추천 이유**: 브라우저를 새로고침하면 우리가 고심해서 담은 관심종목이나 최근 검색어가 사라집니다. 이를 `LocalStorage`와 자동으로 연동해 전원을 껐다 켜도 내 데이터가 그대로 남아있는 '진짜 서비스' 같은 사용자 경험을 제공하는 필수 기술입니다.

- **5. Shadcn/UI 도입 (Tailwind 기반 고수준 컴포넌트)**
    - **학습 내용**: `Headless UI(Radix UI)`의 개념과 Tailwind CSS를 이용한 컴포넌트 커스터마이징 및 확장법.
    - **추천 이유**: "Tailwind를 하나하나 짜기 힘들다"는 고민의 가장 세련된 정답입니다. MUI처럼 이미 완성된 고퀄리티 UI를 제공하면서도, 코드가 내 프로젝트 안에 직접 들어오기 때문에 내 마음대로 모든 것을 수정할 수 있는 실무 최신 트렌드의 컴포넌트 시스템입니다.

- **6. Skeleton UI 구현 (사용자 경험 개선)**
    - **학습 내용**: 데이터 로딩 중 발생할 'Layout Shift(레이아웃 흔들림)'를 방지하는 디자인 패턴과 `Animate-pulse` 애니메이션 활용.
    - **추천 이유**: 데이터가 오기 전까지 텅 빈 화면이나 "로딩 중..."이라는 투박한 글자 대신, 실제 데이터가 들어올 영역의 뼈대를 미리 보여줌으로써 사용자에게 "앱이 빠르고 안정적이다"라는 심리적 확신을 심어주는 디테일의 정점입니다.

- **7. TanStack Query (React Query) 심화 설정**
    - **학습 내용**: `staleTime`, `gcTime(cacheTime)`, `refetchOnWindowFocus` 등 데이터 캐싱 전략 제어.
    - **추천 이유**: 주식 데이터는 1초가 중요하지만, 그렇다고 무한정 서버를 호출하면 리소스 낭비가 심합니다. "얼마나 자주 가져올 것인가?"와 "창을 다시 열었을 때 데이터를 갱신할 것인가?"를 세밀하게 제어해 네트워크 비용은 아끼면서 최신 정보를 유지하는 전략적인 개발자가 되는 과정입니다.

- **9. API 응답 스키마 검증 (Zod)**
    - **학습 내용**: `Zod` 라이브러리를 사용한 런타임 타입 유효성 검사 및 타입 추론 연동.
    - **추천 이유**: 타입스크립트는 빌드 때만 우리를 도와주지만, 실제 서버 응답(`Yahoo Finance`)은 언제든 바뀔 수 있습니다. 데이터의 관문에서 `Zod`로 스키마를 검증하면, 잘못된 데이터가 앱 내부로 들어와 엉뚱한 곳에서 에러를 일으키는 것을 원천 봉쇄할 수 있습니다.

- **10. 이미지 및 라이브러리 최적화 (Dynamic Imports)**
    - **학습 내용**: `next/dynamic`을 사용한 지연 로딩(Lazy Loading) 및 서버 사이드 렌더링(SSR) 조절.
    - **추천 이유**: `ApexCharts` 같은 무거운 라이브러리는 우리 앱의 첫 페이지 로딩을 무겁게 만듭니다. 이를 당장 필요할 때만 불러오도록 최적화하여, 사용자가 우리 주식 페이지에 접속하는 첫 순간의 쾌적함을 비약적으로 상승시키는 고급 최적화 기법입니다.

- **11. 커스텀 훅(Custom Hooks) 패턴화**
    - **학습 내용**: `useStock`, `useInterval`과 같이 재사용 가능한 비즈니스 로직의 완전한 캡슐화.
    - **추천 이유**: 컴포넌트가 UI만 그리게 하고 복잡한 계산이나 데이터 처리 로직은 별도의 훅으로 빼내는 '클린 코드'의 핵심입니다. 이렇게 하면 코드가 읽기 쉬워질 뿐만 아니라, 로직만 따로 떼어내어 테스트하기에도 매우 유리해집니다.

- **12. 웹 접근성(A11y) 및 키보드 네비게이션 심화**
    - **학습 내용**: 웹 접근성 표준(`WAI-ARIA`), 포커스 트래핑(`Focus Trapping`), 그리고 합리적인 키보드 인터랙션 설계.
    - **추천 이유**: 이미 Tab 키 기능을 넣으셨는데, 이를 조금 더 정밀하게 다듬으면 마우스 없이도 검색, 차트 기간 변경, 종목 선택까지 완벽하게 수행할 수 있습니다. 전 세계 모든 사람에게 차별 없는 UX를 제공하는 시니어급의 안목을 기르는 과정입니다.

---

## ✅ 완료된 리스트 (Done)
- **8. 유틸리티 함수 단위 테스트 (Vitest)**
    - **내용**: `TruncateTitle`, `FormatPriceCurrency`(콤마 포맷팅 포함) 등의 핵심 로직 검증 완료.
    - **학습**: Vitest 테스트 환경 구축 및 `describe/it/expect` 패턴을 통한 안정적인 로직 검증법 습득.

---
**👨‍💻 시니어의 조언**
기본기가 탄탄하면 새로운 기술(Zustand, React Query 등)이 나와도 금방 적응할 수 있습니다. 기능을 구현하다가 막히면 언제든 **13, 14번** 내용을 한 번씩 복습해 보세요! 🏁
