# 📋 TODO 리스트

## 🚀 진행 및 학습 예정 항목

1. **StockPage 컴포넌트 컨테이너화 하기**
    - 관심주식 목록 구현하기, 최근 검색목록 리스팅하기, 데이터 폴링 구현하기, STOCK 검색인풋 Header에 고정
2. **UI 라이브러리 도입 검토**: Tailwind를 보완할 Shadcn/UI 또는 MUI 적용하기
3. **주석 내용 복습 및 이해하기**

## 🚀 실무형 주식 앱 고도화 리팩토링 (직접 구현 도전!)

시니어 개발자의 대규모 리팩토링 설계를 바탕으로, 직접 타이핑하며 구조를 익혀보는 단계입니다.

1. **전역 상태 저장소 생성 (`src/store/useStockStore.ts`)**
    - `Zustand`와 `persist` 미들웨어를 활용해 '티커, 검색 기록, 관심 종목'을 관리하는 중앙 저장소를 구축합니다.
2. **검색 로직 커스텀 훅 추출 (`src/hooks/useStockSearch.ts`)**
    - 티커 정제(Trim), 한국 주식 판별(.KS 자동 추가), 스토어 업데이트 등의 비즈니스 로직을 하나로 캡슐화합니다.
3. **`StockSearch` 컴포넌트 리팩토링**
    - `props` 의존성을 제거하고 위에서 만든 훅을 연결하며, Header에 최적화된 세련된 디자인으로 개선합니다.
4. **Header에 전역 검색창 배치**
    - 어느 페이지(설정, 홈 등)에서든 즉시 주식을 검색할 수 있도록 `Header.tsx` 중앙에 검색창을 고정합니다.
5. **`StockPage.tsx` 컨테이너화**
    - 복잡한 검색/유효성 로직을 제거하고, 전역 상태가 바뀌면 자동으로 데이터를 불러와 뿌려주는 '화면 구성' 역할에 집중하게 합니다.
6. **사이드바 리스트 컴포넌트 구현**
    - `RecentSearches.tsx`(최근 검색), `StockWatchlist.tsx`(관심 종목)를 만들어 사이드바에 조립(Composition)합니다.
7. **최종 레이아웃 통합 및 하이드레이션 최적화**
    - 사이드바 1.5배 확장(`w-96`), 본문 여백 조정 및 SSR 환경에서 `persist` 사용 시 발생하는 클라이언트-서버 데이터 불일치 에러를 방지합니다.
8. **실시간 데이터 폴링(Polling) 구현**
    - `useStockSync`에 `refetchInterval`을 설정하여, 유저가 가만히 있어도 30초마다 최신 주가를 가져오게 합니다.

---

### 🌱 기본기 복습 (Foundations)

- **13. JavaScript 현대적 문법 및 배열 메서드 심화**
  - **학습 내용**: `Map`, `Set`의 활용처와 `reduce()`, `filter()`, `some/every()`, `find()` 등 고차 함수의 완벽 숙달.
  - **추천 이유**: 주식 데이터는 거대한 배열의 집합입니다. 서버에서 받은 원본 데이터를 유저가 보기 편한 형태로 가공(Data Transformation)하는 능력은 모든 프론트엔드 개발의 알파이자 오메가입니다.

- **14. 브라우저 렌더링 원리 및 React 리렌더링 조건**
  - **학습 내용**: 브라우저의 CRP(Critical Rendering Path) 과정과 React 컴포넌트가 다시 그려지는 3대 조건(Props 변경, State 변경, 부모 렌더링).
  - **추천 이유**: 주식 차트처럼 데이터가 빈번하게 바뀌는 앱에서는 "왜 리렌더링이 일어나는가?"를 아는 것이 성능 최적화의 첫 걸음입니다. 이를 알아야 `React.memo`나 `useMemo`를 적재적소에 쓸 수 있습니다.

---

### 💡 시니어 추천 학습 항목 (심화 과정)

- **4. Zustand Persist (상태 영속성 관리)**
  - **학습 내용**: `zustand/middleware`의 `persist` 사용법 및 LocalStorage/SessionStorage 선택 전략.
  - **추천 이유**: 브라우저를 새로고침했을 때 내 관심 종목이 사라진다면 유저는 실망할 것입니다. 데이터를 브라우저에 반영구적으로 저장해 '진짜 서비스' 같은 영속성을 부여하는 핵심 기술입니다.

- **5. Shadcn/UI 도입 (Tailwind 기반 고수준 컴포넌트)**
  - **학습 내용**: `Headless UI(Radix UI)`와 Tailwind CSS를 결합해 컴포넌트의 로직(기능)과 스타일을 분리하는 현대적 UI 설계법.
  - **추천 이유**: "Tailwind를 매번 짜기 힘들다"는 고민을 해결해줄 실무 트렌드입니다. MUI의 생산성과 Tailwind의 자유도를 모두 잡은 대안으로, 대규모 팀 프로젝트에서 매우 선호됩니다.

- **6. Skeleton UI 구현 (사용자 경험 개선)**
  - **학습 내용**: 데이터 로드 전후의 레이아웃 차이(Layout Shift)를 최소화하는 디자인 패턴과 `animate-pulse`를 활용한 시각적 효과.
  - **추천 이유**: 로딩 중 뼈대 UI를 보여줌으로써 유저는 앱이 '멈춘 것'이 아니라 '준비 중'임을 인지하게 됩니다. 서비스의 고급스러움을 결정짓는 아주 중요한 마감 디테일입니다.

- **7. TanStack Query (React Query) 심화 설정**
  - **학습 내용**: `staleTime`(데이터가 신선한 시간), `gcTime`(캐시 유지 시간), `refetchOnWindowFocus`(창 재진입 시 갱신) 등.
  - **추천 이유**: 단순히 데이터를 가져오는 것을 넘어 서버 부하를 줄이면서도 최신 데이터를 유지하는 '캐싱 전략'을 세우는 법을 배웁니다. 네트워크 리소스 관리는 시니어급 개발자의 필수 역량입니다.

- **9. API 응답 스키마 검증 (Zod)**
  - **학습 내용**: `Zod` 라이브러리를 사용한 런타임 타입 검사 및 타입 추론(Inference) 연동법.
  - **추천 이유**: 타입스크립트는 빌드 타임에만 작동합니다. 실제 서버에서 오는 잘못된 데이터가 앱을 무너뜨리지 않도록, 입구에서 데이터를 '검역'하는 실무적 방어 프로그래밍을 익힙니다.

- **10. 이미지 및 라이브러리 최적화 (Dynamic Imports)**
  - **학습 내용**: Next.js의 `next/dynamic`을 사용한 대형 라이브러리(ApexCharts 등)의 코드 스플리팅 및 지연 로딩.
  - **추천 이유**: 앱의 첫 실행 속도(LCP)를 비약적으로 높여줍니다. 초기에 필요한 코드만 다운로드하게 함으로써 모바일 환경과 저사양 기기에서도 쾌적한 UX를 보장하는 최적화 기법입니다.

- **12. 웹 접근성(A11y) 및 키보드 네비게이션 심화**
  - **학습 내용**: `Screen Reader` 호환성을 위한 WAI-ARIA 속성 활용과 포커스 트래핑(Focus Trapping) 설계.
  - **추천 이유**: 마우스 없이도 모든 기능을 완벽하게 조작할 수 있는 제품은 '모두를 위한 웹'을 실천합니다. 이는 전문적인 프론트엔드 개발자로서 협업 시 가점을 받을 수 있는 고퀄리티 역량입니다.

---

## ✅ 완료된 리스트 (Done)

- **8. 유틸리티 함수 단위 테스트 (Vitest)**
  - **내용**: `TruncateTitle`, `FormatPriceCurrency` 등의 핵심 로직 검증 완료.
  - **학습**: Vitest 테스트 환경 구축 및 `describe/it/expect` 패턴을 통한 안정적인 로직 검증법 습득.

---
**👨‍💻 시니어의 조언**
학습량이 많아 보이지만, 사실 이 모든 것들은 **"더 빠르고, 더 안전하고, 사용자가 쓰기 편한 앱"**을 만들기 위한 하나의 흐름입니다. 억지로 외우려 하기보다는 기능을 하나씩 구현하며 그 필요성을 체감해 보세요! 🏁
