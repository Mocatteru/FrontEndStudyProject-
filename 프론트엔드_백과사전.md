# 📖 프론트엔드 실무 백과사전 (기초부터 심화까지)

이 문서는 실무에서 **가장 자주 사용하고 중요도가 높은 순서**(필수 생태계 -> 리액트 패턴 -> 테스팅 -> 자바스크립트/타입스크립트 -> 성능 최적화 -> 인프라 및 보안)로 새롭게 정렬된 프론트엔드 백과사전입니다. 각 기술의 존재 이유와 원리(개념설명)를 더 깊이 다루었으며, 모든 항목은 요청하신 양식을 따릅니다.

---

## 🏗️ PART 1. 필수 생태계 및 프로젝트 도구 (Ecosystem & Tools)

프로젝트를 시작할 때 가장 먼저 세팅하며, 매일 작성하는 코드의 기반이 되는 도구들입니다.

### 1. 전역 상태 관리

- **제목**: Zustand (단순하고 강력한 전역 상태 저장소)
- **개념설명**: Redux의 복잡한 보일러플레이트(Action, Reducer, Dispatch)를 제거하고, React Hooks와 동일한 경험을 제공하는 최신 상태 관리 라이브러리입니다. 클라이언트 측 UI 상태(모달 열림/닫힘, 다크모드, 폼 데이터 임시 저장)를 전역으로 관리할 때 매우 직관적이며 가볍습니다.
- **코드예제**:

```typescript
import { create } from 'zustand';

const useStore = create((set) => ({
  count: 0,
  increase: () => set((state) => ({ count: state.count + 1 })),
}));
// 컴포넌트 사용: const increase = useStore((state) => state.increase);
```

### 2. 서버 상태 동기화

- **제목**: TanStack Query (구 React Query)
- **개념설명**: 프론트엔드에서 API 데이터를 가져오고, 캐싱하고, 백그라운드에서 동기화하며, 오래된 데이터(Stale)를 업데이트하는 복잡한 과정을 자동화합니다. 서버의 데이터(Server State)와 클라이언트의 상태(Client State)를 완벽하게 분리하는 현대 웹 개발의 핵심입니다.
- **코드예제**:

```tsx
const { data, isLoading } = useQuery({ 
  queryKey: ['stocks', ticker], 
  queryFn: () => fetchStocks(ticker),
  staleTime: 1000 * 60, // 1분 동안은 캐시된 데이터를 사용 (네트워크 요청 생략)
});
```

### 3. 패키지 매니저 혁신

- **제목**: pnpm (빠르고 효율적인 패키지 매니저)
- **개념설명**: npm이나 yarn은 각 프로젝트마다 수백 MB의 동일한 `node_modules`를 다운로드합니다. pnpm은 하드디스크의 중앙 저장소에 패키지를 한 번만 저장하고, 각 프로젝트에는 '심볼릭 링크(바로가기)'만 걸어주어 설치 속도와 용량을 혁신적으로 아껴줍니다.
- **코드예제**:

```bash
npm i -g pnpm
pnpm install zustand react-hook-form  # 설치 속도가 매우 빠름
```

### 4. 차세대 빌드 도구

- **제목**: Vite (빛처럼 빠른 HMR 브라우저 개발 환경)
- **개념설명**: Webpack이 프로젝트가 커질수록 빌드 속도가 기하급수적으로 느려지는 현상을 타파했습니다. 브라우저의 네이티브 ES 모듈을 활용하여 소스코드를 미리 번들링하지 않고 바로 서빙하므로, 파일을 수정하자마자 브라우저에 0.1초 만에 반영(HMR)됩니다.
- **코드예제**:

```bash
pnpm create vite my-project --template react-ts
```

### 5. 폼(Form) 관리와 유효성 검증

- **제목**: React Hook Form & Zod
- **개념설명**: 기존 React 폼은 입력할 때마다 화면을 리렌더링해 성능이 저하됐습니다. React Hook Form은 비제어(Uncontrolled) 컴포넌트 방식을 사용해 렌더링을 방지하며, Zod 스키마를 결합해 복잡한 이메일/비밀번호 규칙을 코드 몇 줄로 완벽히 검증하고 에러 메시지를 띄워줍니다.
- **코드예제**:

```tsx
const schema = z.object({ email: z.string().email('이메일 형식이 아닙니다.') });
const { register, handleSubmit, formState: { errors } } = useForm({
  resolver: zodResolver(schema)
});
```

### 6. 범용 스타일링 패러다임

- **제목**: Tailwind CSS (유틸리티 우선 CSS)
- **개념설명**: CSS 파일과 컴포넌트 파일을 오가며 클래스 이름을 짓는 고통(`.btn-primary-wrapper`)을 없앴습니다. HTML 엘리먼트 안에 미리 정의된 속성(`bg-blue-500`, `flex`)을 조합하여 빠르고 일관된 디자인을 그려내며, 빌드 시 안 쓰는 클래스는 모두 날려 용량도 작습니다.
- **코드예제**:

```tsx
<div className="flex justify-between items-center bg-gray-100 p-4 rounded-xl">
  <span className="text-blue-500 font-bold hover:text-blue-600">Tailwind</span>
</div>
```

### 7. API 통신 중앙 통제

- **제목**: Axios / Fetch Interceptor (에러 및 인증 추상화)
- **개념설명**: 흩어져 있는 API 호출 로직을 하나로 모아, 요청을 보내기 전 헤더에 무조건 인증 토큰을 꽂아주거나, 응답에서 401(권한 없음) 에러가 발생하면 알아서 로그인 페이지로 튕겨내도록 하는 '검문소' 역할을 합니다.
- **코드예제**:

```typescript
api.interceptors.response.use(
  (res) => res,
  (error) => {
    if (error.response?.status === 401) alert('다시 로그인해주세요.');
    return Promise.reject(error);
  }
);
```

### 8. 코드 품질 유지

- **제목**: ESLint & Prettier
- **개념설명**: 여러 명의 개발자가 작업하더라도 한 사람이 짠 것처럼 코드를 일치시키는 도구입니다. Prettier가 띄어쓰기, 줄바꿈, 콤마 등 '타이포그래피/스타일'을 강제한다면, ESLint는 사용하지 않는 변수 선언이나 잠재적인 버그 등 '문법적 품질'을 검사하고 차단합니다.
- **코드예제**:

```json
// .eslintrc.json
"rules": {
  "react-hooks/exhaustive-deps": "warn", // useEffect 의존성 배열 누락 경고
  "no-console": "error" // console.log 배포 금지
}
```

---

## 🧩 PART 2. 리액트 핵심 설계 패턴 (React Core Patterns)

라이브러리 세팅이 끝난 후, 실무 컴포넌트를 작성할 때 숨 쉬듯 사용하는 로직 설계 패턴입니다.

### 9. 비즈니스 로직 분리

- **제목**: 커스텀 훅 (Custom Hooks)
- **개념설명**: 컴포넌트(View)에는 오직 UI 구조만 남기고, 데이터를 가져오거나 가공하는 복잡한 상태 로직(Controller)은 `use-`로 시작하는 별도의 함수로 뽑아내는 패턴입니다. 코드가 깔끔해지고 로직의 재사용성이 비약적으로 상승합니다.
- **코드예제**:

```tsx
function useWindowWidth() {
  const [width, setWidth] = useState(window.innerWidth);
  useEffect(() => { /* 리사이즈 이벤트 로직 */ }, []);
  return width; 
}
// 뷰 컴포넌트: const width = useWindowWidth(); 
```

### 10. 가독성 극대화

- **제목**: 얼리 리턴 (Early Return)
- **개념설명**: 로딩 중이거나 에러가 났을 때 보여줄 화면을 컴포넌트 최상단에서 우선적으로 `return`시켜 버리는 기법입니다. 아래로 내려갈수록 중첩되는 `if-else` 블록을 제거하여, 핵심 UI가 그려지는 코드가 들여쓰기 없이 깔끔하게 배치되도록 합니다.
- **코드예제**:

```tsx
if (isLoading) return <LoadingSpinner />;
if (error) return <ErrorMessage message={error.message} />;

// 예외를 모두 통과한 진짜 UI
return <RealContent data={data} />;
```

### 11. 재사용성 및 드릴링 해소

- **제목**: 컴포넌트 합성 (Composition & children)
- **개념설명**: 부모가 10단계 아래의 자식에게 데이터를 넘기는 현상(Props Drilling)을 막기 위해, 부모 컴포넌트가 자식 컴포넌트를 태그 형태로 직접 열어서 사이에 UI를 끼워 넣는(`children`) 유연한 설계 패러다임입니다.
- **코드예제**:

```tsx
function Layout({ header, content }) {
  return <div className="h-screen"><nav>{header}</nav><main>{content}</main></div>;
}
// Props 없이 직접 조립
// <Layout header={<Header />} content={<Page />} />
```

### 12. 메모리 및 사이드이펙트 정리

- **제목**: useEffect 클린업 (Cleanup)
- **개념설명**: 리액트는 컴포넌트가 화면에서 사라지거나 재렌더링 될 때, 이전에 등록해 둔 타이머나 브라우저 이벤트 리스너를 자동으로 지워주지 않습니다. `useEffect`가 반환하는 함수 내부에서 이를 직접 지워야 성능 저하와 좀비 프로세스 버그를 막을 수 있습니다.
- **코드예제**:

```tsx
useEffect(() => {
  const timer = setInterval(() => console.log('째깍'), 1000);
  return () => clearInterval(timer); // 언마운트 시 지우기
}, []);
```

### 13. 불필요한 렌더링 방어

- **제목**: useMemo & useCallback
- **개념설명**: 리액트는 부모가 렌더링되면 요소가 똑같더라도 모든 자식을 다시 그립니다. `useMemo`는 무거운 계산의 '결과값'을 재사용하고, `useCallback`은 '함수'를 재사용하여 자식 컴포넌트가 쓸데없이 리렌더링되는 현상을 캐싱을 통해 방어합니다. 주의할 점은 캐싱 자체도 소량의 메모리를 소모하므로 꼭 필요할 때만 써야 합니다.
- **코드예제**:

```tsx
const memoizedData = useMemo(() => heavyCalculation(data), [data]);
const memoizedFn = useCallback(() => onSubmit(data), [data]);
```

---

## 🛠️ PART 3. 품질 검증 및 컴포넌트 주도 개발 (Testing & CDD)

안정적인 상용 서비스를 운영하기 위한 필수 품질 관리 레이어입니다.

### 14. 컴포넌트 독립 개발

- **제목**: Storybook & CDD (Component Driven Development)
- **개념설명**: 화면 전체를 한 번에 만드는 것이 아니라, 페이지와 분리된 'Storybook' 환경에서 개별 버튼이나 입력창을 하나씩 그려 작동을 확인하는 방식입니다. 기획자 및 디자이너가 코드 없이도 다양한 상태(성공, 에러, 로딩)의 컴포넌트 UI를 확인할 수 있는 살아있는 디자인 시스템 사전을 만듭니다.
- **코드예제**:

```tsx
// Button.stories.tsx
export default { component: Button };
export const Primary = { args: { primary: true, label: '추가하기' } };
export const Disabled = { args: { disabled: true, label: '권한 없음' } };
```

### 15. 사용자 관점 UI 검증

- **제목**: React Testing Library (RTL)
- **개념설명**: 컴포넌트 내부의 변수가 어떻게 바뀌는지를 테스트하는 것이 아니라, 사용자가 겪는 현실 그대로 "이 버튼을 클릭하면 '성공'이라는 글자가 화면에 나타나는가?"를 테스트하는 현대 프론트엔드 테스트의 표준입니다.
- **코드예제**:

```tsx
test('로그인 성공 메시지가 표출된다', () => {
  render(<SubmitButton />);
  fireEvent.click(screen.getByText('로그인'));
  expect(screen.getByText('성공!')).toBeInTheDocument();
});
```

### 16. 핵심 로직 검증

- **제목**: Jest (자바스크립트 단위 테스트)
- **개념설명**: 화면(UI)이 없는 순수한 자바스크립트 함수(ex: 날짜 포맷 함수, 가격 계산 로직)들이 예상되는 입력값에 대해 정확히 기대하는 출력값을 반환하는지 검사합니다.
- **코드예제**:

```typescript
test('가격을 콤마로 포맷팅한다', () => {
  expect(formatPrice(10000)).toBe('10,000');
});
```

### 17. 실사용자 흐름 방어

- **제목**: Cypress / Playwright (E2E 테스트)
- **개념설명**: 브라우저를 백그라운드에 띄워 봇(Bot)이 실제로 사이트에 접속해 "로그인 -> 상품 장바구니 담기 -> 결제하기" 과정을 자동화하여 수행합니다. 아주 중요하고 필수적인 사용자 기능(End to End)이 붕괴되지 않았는지 배포 전 마지막으로 점검합니다.
- **코드예제**:

```javascript
it('장바구니 담기', () => {
  cy.visit('/products/1');
  cy.get('[data-cy="add-to-cart"]').click();
  cy.contains('장바구니에 담겼습니다.').should('be.visible');
});
```

---

## 🚀 PART 4. 리액트 심화 아키텍처 (Advanced Architecture)

거대해지는 프로젝트를 다루거나, 프레임워크 한계를 깰 때 필요한 구조들입니다.

### 18. 프레임워크 렌더링 충돌 극복

- **제목**: Hydration Mismatch 해결
- **개념설명**: Next.js 같은 SSR(서버사이드 렌더링) 환경에서, 서버가 미리 그려준 정적 HTML 구조와 브라우저가 클라이언트 측 데이터(ex. 로컬스토리지, 브라우저 크기)를 읽고 그리려던 가상 DOM 구조가 다르면 에러가 발생합니다. 브라우저 마운트 전까지는 클라이언트 렌더링을 지연시켜 회피합니다.
- **코드예제**:

```tsx
const [isMounted, setIsMounted] = useState(false);
useEffect(() => setIsMounted(true), []);

// 서버/클라이언트 차이를 유발하는 UI는 마운트 후 렌더링
if (!isMounted) return null; 
return <div>{window.innerWidth}px</div>;
```

### 19. 전역 에러 제어

- **제목**: Error Boundaries (에러 경계)
- **개념설명**: 렌더링 도중 배열이 undefined라 앱 전체가 다운되는 치명적인 크래시(백화 현상)가 발생할 때, 최상단까지 에러가 번지지 않도록 막고 해당 영역만 '문제가 발생했습니다' 텍스트로 우아하게 대체합니다.
- **코드예제**:

```tsx
import { ErrorBoundary } from 'react-error-boundary';

<ErrorBoundary fallback={<div className="error">데이터를 부르지 못했습니다</div>}>
  <ChartComponent />
</ErrorBoundary>
```

### 20. 체감 로딩 시간 제거

- **제목**: Optimistic UI (낙관적 업데이트)
- **개념설명**: 사용자가 '좋아요' 버튼을 불렀을 때 서버에서 DB 처리를 하고 OK 응답이 올 때까지 기다리면 굉장히 굼떠 보입니다. 요청이 서버로 가는 즉시 화면 상태부터 먼저 성공으로 바꾼 뒤, 만약 요청이 실패하면 사용자 모르게 슬그머니 상태를 롤백시킵니다.
- **코드예제**:

```tsx
setLikes(prev => prev + 1); // 먼저 화면 갱신 성공 처리
mutation.mutate(postId, {
  onError: () => setLikes(prev => prev - 1) // 문제 발생 시 꼼작없이 이전으로 복구
});
```

### 21. 유연한 마크업 및 Context 활용

- **제목**: Compound Components (복합 컴포넌트)
- **개념설명**: `<select>` 안에 `<option>`들이 각각의 위치에 상관없이 알아서 부모와 소통하는 것처럼, 자식 요소들이 부모의 Context API를 통해 은밀하게 데이터를 교환함으로써 개발자가 자유롭게 태그 배치를 바꿀 수 있게 하는 고급 UI 설계법입니다.
- **코드예제**:

```tsx
<Dropdown>
  <Dropdown.Toggle>메뉴 열기</Dropdown.Toggle>
  <Dropdown.Menu>
    <Dropdown.Item>선택 1</Dropdown.Item>
  </Dropdown.Menu>
</Dropdown>
```

### 22. DOM 계층 한계 돌파

- **제목**: React Portal (포털)
- **개념설명**: 부모 컴포넌트에 CSS `overflow: hidden`나 `z-index`가 잡혀있으면 모달 창이 부모 박스 크기를 벗어나지 못하고 잘려 보입니다. 포털을 사용하면 로직은 컴포넌트 안에 두되, 실제 화면에 그려지는 위치만 강제로 최상단 `<body>` 밑으로 순간 이동시킵니다.
- **코드예제**:

```tsx
import { createPortal } from 'react-dom';

function Modal({ children }) {
  const root = document.getElementById('modal-root');
  return createPortal(<div className="fixed inset-0">{children}</div>, root);
}
```

### 23. 제어권의 위임

- **제목**: Control Props Pattern
- **개념설명**: 컴포넌트가 알아서 닫히고 열리도록 내부 상태(`useState`)를 가질 수도 있지만, 간혹 외부 부모가 값을 강제로 주입할 때는 내부 상태를 무시하고 부모 명령에 따르도록 유연하게 설계하는 패턴입니다.
- **코드예제**:

```tsx
function Toggle({ isOn: controlledIsOn, onChange }) {
  const [internalIsOn, setInternalIsOn] = useState(false);
  // 외부 props가 넘어오면 그것을 우선순위로, 아니면 내부 상태를 사용
  const isOn = controlledIsOn !== undefined ? controlledIsOn : internalIsOn;
}
```

---

## 🔰 PART 5. 모던 자바스크립트 딥 다이브 (Modern JS Deep Dive)

모든 리액트 오류의 근본 원인이 되는 자바스크립트 코어입니다.

### 24. 불변성과 재렌더링의 뇌관

- **제목**: 원시 타입과 참조 타입 (Primitive vs Reference)
- **개념설명**: 문자열이나 숫자는 값이 바로 저장되므로 비교가 쉽지만, 객체(`{}`)와 배열(`[]`)은 힙 메모리의 '주소'만 저장됩니다. 상태 일부를 바꿀 때 완전히 새로운 통(`...spread` 연산자)에 복사해서 완전히 다른 주소값을 만들어 내지 않으면, 리액트는 "주소가 똑같네? 안 변했네?" 하고 렌더링을 완전히 거부합니다.
- **코드예제**:

```typescript
const obj = { v: 1 };
const bad = obj; bad.v = 2; // obj 주소와 같음 -> 리액트 인식 불가
const good = { ...obj, v: 2 }; // 새 주소 할당 -> 리액트 재렌더링 촉발!
```

### 25. 비동기 실행의 교통정리

- **제목**: Event Loop & Microtask Queue
- **개념설명**: 자바스크립트는 일꾼(스레드)이 단 하나입니다. 코드를 읽다가 인터넷 통신이나 타이머를 만나면 대기실로 던져버리고 다음 코드를 실행합니다. 타이머(Macrotask)와 Promise(Microtask)가 모두 완료될 경우, 항상 Promise가 압도적인 우선순위를 가져 새치기하여 실행됩니다.
- **코드예제**:

```typescript
setTimeout(() => console.log('3. 일반 대기실 처리 (Macro)'), 0);
Promise.resolve().then(() => console.log('2. VIP 대기실 처리 (Micro)'));
console.log('1. 당장 실행 (Call Stack)');
```

### 26. 은닉과 기억의 마법

- **제목**: 클로저 (Closure)
- **개념설명**: 함수가 실행이 끝나서 죽어도, 함수 안에 있던 내부 함수가 외부 함수의 변수들을 여전히 기억하고 접근할 수 있는 현상입니다. 접근이 불가능한 프라이빗(`private`) 변수를 흉내 내거나 커스텀 훅 안에 상태를 가둘 때 사용됩니다.
- **코드예제**:

```typescript
function bank() {
  let money = 0; // 외부에서는 죽어도 조작 불가
  return { save: (amt) => { money += amt; return money; } };
}
```

### 27. 동시 타격 비동기 전술

- **제목**: Promise.all() 병렬 처리
- **개념설명**: 메인 피드 API(2초), 유저 정보 API(3초)를 `await`로 순서대로 호출하면 총 5초가 걸립니다. `Promise.all`로 두 개를 한 번에 쏘면 긴 쪽인 3초 만에 두 데이터가 동시에 도착해 앱의 반응성이 극적으로 빨라집니다.
- **코드예제**:

```typescript
const [feed, user] = await Promise.all([fetchFeed(), fetchUser()]);
```

### 28. 호이스팅의 함정

- **제목**: TDZ (Temporal Dead Zone)
- **개념설명**: `var` 변수는 선언도 전에 쓰면 `undefined`가 튀어나오지만, 최신 구문인 `let`과 `const`는 코드 파서가 인지는 하되 실제 값이 초기화되는 줄 전까지 접근하려 하면 Reference 에러로 애플리케이션을 차단하여 안전성을 지킵니다.
- **코드예제**:

```typescript
// console.log(title); // 에러! (TDZ 사각지대)
const title = '프론트엔드';
```

### 29. 무거운 연산의 분리 구축

- **제목**: Web Workers
- **개념설명**: 메인 스레드가 UI 그림 그리기와 이벤트를 처리하는 동안, 10초짜리 엑셀 데이터 파싱 연산을 시키면 웹사이트가 아예 멈춰버립니다(Freezing). 워커를 쓰면 화면 뒤(Background) 스레드에서 무거운 연산을 처리하고 결과 톡만 메인 스레드로 쏴줍니다.
- **코드예제**:

```javascript
const worker = new Worker('heavyTask.js');
worker.postMessage(hugeData);
worker.onmessage = (e) => setChartData(e.data);
```

---

## 🛡️ PART 6. 타입스크립트 실무 정복 (TypeScript 실전)

개발 환경에서 오류를 미리 경고하여 런타임 붕괴를 예방하는 방어 기법입니다.

### 30. 안전한 객체 추론

- **제목**: Discriminated Unions (식별된 유니온)
- **개념설명**: 데이터가 때에 따라 에러 객체일 수도 있고, 정상적인 객체일 수도 있을 때 `type`이나 `status` 같은 공통 스위치 속성을 박아둡니다. 그러면 TS는 해당 속성을 스위치문으로 열어보면 나머지 속성들이 뭐가 들어올지 100% 확신하며 자동 완성을 제공합니다.
- **코드예제**:

```typescript
type Result = { status: 'success', data: string } | { status: 'error', code: number };
if (res.status === 'success') { res.data } // TS는 res에 code가 없음을 보장함
```

### 31. 무한 변신 다형성

- **제목**: Generics (제네릭)
- **개념설명**: 데이터를 담는 박스의 기능은 똑같은데, 박스 안에 문자열이 들어올지 숫자가 들어올지 함수 호출 직전까지 모를 때 사용합니다. 함수의 타입을 고정하지 않고 변수(`<T>`)처럼 취급하여 재사용성을 미치게 끌어올립니다.
- **코드예제**:

```typescript
function getFirst<T>(arr: T[]): T { return arr[0]; }
const n = getFirst<number>([1, 2, 3]); // 리턴 타입이 자동으로 number가 됨
```

### 32. 불확실성과의 타협

- **제목**: any vs unknown vs never
- **개념설명**:
  - `any`: 타입스크립트를 쓰기 포기하는 비상 탈출구 (극양날의 검).
  - `unknown`: "뭐가 올지 나도 모르니까, 쓸 거면 `typeof`로 타입을 증명하고 써!"라고 강제하는 안전장치.
  - `never`: "절대로 이 리턴값은 발생할 수 없다"는 것을 명시(오류 Throw 함수 등).
- **코드예제**:

```typescript
let value: unknown = getDynamicValue();
// value.toUpperCase(); // 에러 방어 발동
if (typeof value === 'string') value.toUpperCase(); // 타입 가드 통과시에만 허가
```

### 33. 기존 명세서 편집

- **제목**: Utility Types (Pick, Omit, Partial)
- **개념설명**: 다른 백엔드 개발자가 작성한 거대한 구조체 타입에서 아이디와 이름만 빼서 화면에 쓰고 싶을 때 사용합니다. 타입 복사 붙여넣기를 줄이고 일관성을 유지합니다.
- **코드예제**:

```typescript
interface User { id: string, name: string, age: number, token: string }
type SafeUser = Omit<User, 'token'>; // 토큰만 제거
type UpdateUserDto = Partial<User>; // 모든 요소를 필수가 아닌 선택적(?)으로 변경
```

### 34. 엄격한 불변성

- **제목**: as const (리터럴 고정 타입)
- **개념설명**: 배열을 선언하면 타입스크립트는 대충 `string[]`일 것이라 여유 있게 판단합니다. 뒤에 `as const`를 달아주면 "원소 추가 절대 금지, 값도 이것 외엔 안 됨"이라며 아주 엄격하고 불변하는 고정 세트로 인지합니다.
- **코드예제**:

```typescript
const STATUS_LIST = ["IDLE", "RUNNING", "ERROR"] as const;
type Status = typeof STATUS_LIST[number]; // 단순히 string이 아닌 3개의 문자열로 타입 강제
```

---

## 🏎️ PART 7. 프론트엔드 성능 최적화 (Web Performance)

서비스의 속도뿐 아니라, 비즈니스 지표(이탈률 등)에 직결되는 궁극의 튜닝입니다.

### 35. 영리한 스크롤 감지

- **제목**: Intersection Observer API
- **개념설명**: 페이지 아래에 숨겨져 있는 이미지나 다음 페이지의 요소를 감지할 때, 수많은 이벤트 부하를 던지는 브라우저의 전역 `Scroll` 이벤트를 리스닝하는 대신 사용할 수 있는 현대적 브라우저 내장 도구입니다 성능 낭비 없는 무한 스크롤과 레이지 로딩의 핵심 마법입니다.
- **코드예제**:

```tsx
const observer = new IntersectionObserver(([entry]) => {
  if (entry.isIntersecting) fetchNextPage(); // 박스가 화면에 등장할 때만 실행
});
observer.observe(bottomTracerRef.current);
```

### 36. 무차별 공격 방어선

- **제목**: Debounce & Throttle
- **개념설명**: 성능을 위해 사용자 이벤트를 솎아내는 테크닉입니다. '디바운스'는 검색창에 키보드를 다다닥 누르고 손을 떼면 잠깐 기다렸다가 백엔드 API를 딱 한 번 쏘고, '쓰로틀'은 윈도우 창 크기를 늘릴 때 0.5초 등 지정된 시간에 한 번씩만 통과시키는 등 콜백 수를 제한합니다.
- **코드예제**:

```tsx
const sendSearchQuery = debounce((text) => api.search(text), 400); // 400ms 침묵 시 발동
```

### 37. 초기 덩치 빼기

- **제목**: Dynamic Imports & Code Splitting
- **개념설명**: 사용자가 첫 화면(메인 대시보드)에 접속했는데, 당장 쓰지도 않을 설정 화면이나 모달 안의 수십 MB 짜리 에디터 코드를 다 다운로드하도록 강제하면 이탈합니다. 리액트의 `lazy` (혹은 Next의 `dynamic`)를 써서 모달을 누르는 순간 쪼개놓은 코드를 지연 로딩합니다.
- **코드예제**:

```tsx
import dynamic from 'next/dynamic';
// 다운로드를 늦추고, 서버에서도 미리 그리지 말라고 명령
const HeavyChart = dynamic(() => import('./Chart'), { ssr: false });
```

### 38. 렌더 파이프라인 학살 방지

- **제목**: Layout Thrashing 방어 (Reflow / Repaint 최소화)
- **개념설명**: 브라우저에게 "이 엘리먼트 높이 가져와!"(Read) 한다음 곧장 "저 박스 너비를 넓혀!"(Write) 하는 행위를 루프문에서 반복하면, 브라우저가 화면을 갱신하느라 아무 생각도 못합니다. 읽을 값을 모두 캐시하고 일괄적으로 스타일을 써야(Batch) 깜빡임과 렉을 방지할 수 있습니다.
- **코드예제**:

```javascript
// Good: 모든 엘리먼트의 높이를 먼저 모두 읽은 뒤에 루프 밖에서 일괄적으로 조작한다.
```

### 39. 부드러운 애니메이션 엔진

- **제목**: requestAnimationFrame (rAF)
- **개념설명**: CSS로 구현하기 힘든 캔버스 위 애니메이션이나 자바스크립트 스크롤을 `setTimeout`으로 그리면 브라우저 화면 갱신 주기(60Hz, 144Hz)와 어긋나서 버벅거립니다. rAF는 브라우저가 다음 장면을 그릴 준비가 된 완벽한 타이밍에 예쁘게 끼어들어 연산을 실행하게 위임합니다.
- **코드예제**:

```javascript
function step() {
  updateGameObj();
  requestAnimationFrame(step); // 1초에 60번가량 완벽한 시점에 브라우저가 호출해줌
}
```

### 40. 잔가지 치기

- **제목**: Tree Shaking (죽은 코드 제거)
- **개념설명**: 거대한 유틸리티 라이브러리(Lodash 등)를 임포트할 때, 내가 실제로 쓰는 함수는 한두 개에 불과할 수 있습니다. 웹팩(Webpack)이나 롤업(Rollup)같은 번들러가 이를 파악하여 사용되지 않는 함수는 상용 배포 코드에서 완전히 갈아 없애서 파일 용량을 극적으로 줄여냅니다.
- **코드예제**:

```javascript
import { isEmpty } from 'lodash-es'; // 전체가 아닌 특정 모듈 트리만 가져와 쉐이킹 유도
```

### 41. 브라우저 여유 활용

- **제목**: Resource Hinting (Preload / Prefetch)
- **개념설명**: 사용자가 다음 행동으로 뭘 할지 알 수 있을 때 최적화하는 기술입니다. `Preload`는 "화면 렌더링에 이 웹 폰트가 핵심이니 무조건 빨리 받아 놔!" 이고, `Prefetch`는 "사용자가 이 링크 누를 확률이 높으니 백그라운드에서 다음 화면 스크립트 좀 미리 다운 받아놔" 하는 선제적 힌팅입니다.
- **코드예제**:

```html
<link rel="preload" href="/fonts/Core.woff2" as="font" type="font/woff2" crossorigin>
<link rel="prefetch" href="/js/next-page-chunk.js">
```

### 42. 구글 SEO 지표

- **제목**: Web Vitals
- **개념설명**: 웹의 성능을 나타내는 산업 3대 표준수치입니다. LCP (페이지 내 가장 큰 이미지가 로딩되는 시간), FID (사용자가 버튼을 클릭했을 때 브라우저가 응답하기까지의 지연 시간), CLS (이미지가 뒤늦게 등업하며 기사 글씨가 아래로 덜컹하고 밀리는 폭)를 뜻합니다.
- **코드예제**:

```tsx
import { onCLS, onLCP, onFID } from 'web-vitals';
onCLS(console.log); // 점수가 나쁘면 이미지에 구체적인 height 지정해 CLS 폭 개선 조치 진행
```

### 43. 렌더링 프로세스

- **제목**: Critical Rendering Path (CRP)
- **개념설명**: HTML 파싱 -> DOM/CSSOM 트리 생성 -> Render Tree 변환 -> 위치 계산(Layout/Reflow) -> 색칠(Paint) -> 최종 합성(Composite)으로 이어지는 브라우저의 그리기 파이프라인. Layout을 건드르는 CSS 속성은 성능상 최악이며, Transform 등 Composite만 건드리는 애니메이션이 최고입니다.
- **코드예제**:

```css
/* Bad: width는 레이아웃을 다시 일으켜 느림 */
/* Good: transform은 독립된 GPU 컴포지트 단계만 실행하여 부드러움 */
.slide { transform: translateX(100px); }
```

---

## 🌐 PART 8. 네트워크, 브라우저 및 데이터 보안 (Network & Security)

현대 웹 플랫폼이 통신하고, 저장하고, 외부 위협으로부터 방어하는 방법들입니다.

### 44. 외부 통신 거부권

- **제목**: CORS (Cross-Origin Resource Sharing)
- **개념설명**: `http://localhost:3000` (나의 개발 환경)에서 `https://api.github.com` (백엔드서버)으로 데이터를 긁어오려 할 때 브라우저가 차단하는 정책. 만일 이 정책이 없다면 악성 스크립트가 해커 맘대로 유저 데이터를 뽑아가 뒷단에 전송할 수 있습니다.
- **코드예제**:

```http
// 백엔드 응답 헤더에 아래 값이 있어야 브라우저가 통과시켜 줌
Access-Control-Allow-Origin: http://localhost:3000
```

### 45. 인증의 이원화

- **제목**: JWT (Access & Refresh 토큰 전략)
- **개념설명**: 세션을 서버에 저장하지 않는 토큰 방식의 취약점은 해커가 토큰을 탈취하면 속수무책이란 점입니다. Access 토큰(신분증)은 15분 정도로 수명을 아주 짧게 주고 만료시키며, 클라이언트 비밀 창고에 보관된 긴 수명의 Refresh 토큰(인감)을 이용해 갱신을 받습니다.
- **코드예제**:

```typescript
axios.interceptors.response.use(res => res, async err => {
  if (err.response.status === 401) { // 엑세스 토큰 죽음
    const newAT = await reissueAccessTokenByRefresh();
    // 새 토큰 들고 직전 요청 재도전
  }
});
```

### 46. 대표적인 프론트엔드 해킹

- **제목**: XSS & CSRF 방어
- **개념설명**:
  - **XSS**: 게시판 글에 정상 텍스트 대신 JS 스크립트(`<script>쿠키해킹로직</script>`)를 심어, 이 글을 열람하는 모든 자의 데이터를 빼냅니다.
  - **CSRF**: 로그인된 상태(쿠키 등)의 위조된 요청을 속여 보내 비밀번호를 바꾸거나 송금을 일으킵니다. 이를 방어하기 위해 프론트엔드에서는 HTML을 정화하고, SameSite 속성을 활용합니다.
- **코드예제**:

```tsx
import DOMPurify from 'dompurify';
// 외부 HTML을 렌더링할 땐 반드시 살균(Sanitize) 필수
<div dangerouslySetInnerHTML={{ __html: DOMPurify.sanitize(dirtyHtml) }} />
```

### 47. 데이터의 주소지

- **제목**: Browser Storage (Cookie vs Local vs Session)
- **개념설명**: 브라우저 안에 정보를 두는 3개의 방입니다.
  - `Cookie`: 매번 API 요청마다 네트워크를 같이 타고 자동으로 전송되어 서버와 인증하기 좋습니다(HttpOnly 속성을 걸면 JS로 탈취 불가해 가장 안전).
  - `LocalStorage`: 탭을 닫아도 평생 유지되어 앱 셋팅(테마 등) 보관에 적합합니다.
  - `SessionStorage`: 탭이 닫히면 사라지는 일회성 정보 보관용입니다.
- **코드예제**:

```javascript
sessionStorage.setItem('current_page', 2); // 탭 닫으면 초기화
```

### 48. 실시간 파이프라인

- **제목**: WebSockets vs SSE (Server-Sent Events)
- **개념설명**: 채팅앱을 만들려면 클라이언트도 보내고 서버도 보내는 `WebSocket`(양방향)이 필수입니다. 하지만 만약 주식 차트, 실시간 뉴스 속보처럼 '서버가 클라이언트에게 일방적으로 떠들기만' 해야 한다면, 훨씬 통신 오버헤드가 적고 구현이 쉬운 `SSE` 방식을 씁니다.
- **코드예제**:

```javascript
const evtSource = new EventSource('/api/stocks');
evtSource.onmessage = (event) => console.log('주가 폭등!', event.data);
```

### 49. 최후의 보안 헤더

- **제목**: Content Security Policy (CSP)
- **개념설명**: XSS 공격을 막는 브라우저 차원의 최후의 벽입니다. 백엔드에서 내려주는 보안 헤더로서, "이 웹사이트는 우리의 CDN 도메인 외에 삽입된 다른 이상한 이미지 파일이나 스크립트 파일은 아예 다운로드조차 금지시켜" 라고 엄격한 제약을 겁니다.
- **코드예제**:

```http
Content-Security-Policy: default-src 'self'; script-src https://my-trusted-cdn.com;
```

---

## 🔮 PART 9. 현대적 웹 아키텍처와 시맨틱 트렌드 (Modern Web & Semantics)

초대형 서비스 구조, 배포, 접근성 및 UX를 관통하는 차세대 개념 모음입니다.

### 50. 저장소 중앙집중화

- **제목**: 모노레포 아키텍처 (Monorepo)
- **개념설명**: 회사의 어드민 사이트, 유저 사이트, 공용 UI 컴포넌트 묶음을 각자의 Github 레포에 두면 의존성 관리가 대참사가 벌어집니다. 이를 1개의 큰 깃허브 저장소(Monorepo) 내부에 합치고, 폴더를 패키지 모듈 단위로 구성하되 Turborepo 체계 등을 이용해 빌드하고 소스를 통합 공유합니다.
- **코드예제**:

```json
// 루트 package.json의 workspaces 선언
"workspaces": [ "apps/*", "packages/ui" ]
```

### 51. 로봇 군단의 개발 검수

- **제목**: CI/CD (안전하고 자동화된 배포 플랫폼)
- **개념설명**: 개발자가 코드 푸시(Push) 한 번 하면, 백그라운드의 컴퓨터 부대가 깨어나 코드를 ESLint로 문법 검토하고, Jest로 다 부숴지지 않았나 테스트하며, 완벽할 시에만 상용 서버(Vercel, AWS 등) 위로 새 집을 지어 올려버리는 기법(Github Actions 활용).
- **코드예제**:

```yaml
# github-actions 스크립트 생략 예
on: [push]
jobs:
  test: run npm run test
```

### 52. 윈도우/맥 타협의 종결자

- **제목**: Docker (컨테이너화)
- **개념설명**: 개발자의 로컬 OS나 Node 버전이 달라서 터지는 환경 충돌을 예방합니다. 프로젝트 자체가 동작하기 위한 가상 운영체제(Linux) 공간, 즉 컨테이너 박스를 배에 실어 보내면, 전 세계 어디의 서버 엔진이든 똑같은 구동 환경을 보장합니다.
- **코드예제**:

```dockerfile
FROM node:18-alpine
COPY package.json ./
RUN pnpm install
CMD ["pnpm", "dev"]
```

### 53. 앱과의 경계 붕괴

- **제목**: PWA (Progressive Web Apps)
- **개념설명**: 그냥 크롬 브라우저로 들어온 홈페이지인데 스마트폰 바탕화면에 다운로드해서 아이콘이 박힐 수 있게 하며, 비행기 모드(오프라인)에서도 서비스 워커가 캐싱된 사이트를 띄워줍니다. 게다가 네이티브 앱 전유물이었던 푸시 알람까지 쏴주는 기술입니다.
- **코드예제**:

```json
// manifest.json
{ "name": "내 앱", "start_url": "/", "display": "standalone" }
```

### 54. SEO를 위한 언어

- **제목**: 시맨틱 마크업 (Semantic HTML)
- **개념설명**: 기계와 검색엔진(구글봇 등)을 위한 예절입니다. 네비게이션을 그릴 때 모양이 같다고 파란색 막대 `<div>`를 쓰지 말고 반드시 `<nav>`라는 뜻을 가진 뼈대 태그를 사용해야 구글이 중요도를 높게 쳐주어 최상단 검색 노출에 유리해집니다.
- **코드예제**:

```html
<header><h1>메인 주제</h1></header>
<main><article>핵심 콘텐츠</article></main>
<footer>카피라이트</footer>
```

### 55. 장벽 없는 웹

- **제목**: ARIA 특성과 웹 접근성 (a11y)
- **개념설명**: 화면을 눈으로 보지 못해 화면 낭독기(스크린 리더)를 사용하는 시각 장애인 유저를 위해 길을 놓아줍니다. 의미 없는 버튼(`div` 닫기버튼) 등에 `role="button"`과 `aria-label="닫기"`를 부여하여 오디오로 정체를 통보할 수 있게 만듭니다.
- **코드예제**:

```html
<div role="button" aria-expanded="false" aria-label="메뉴 열기" onClick={open}>
  <Icon type="hamburger" aria-hidden="true" />
</div>
```

### 56. 글로벌 시장 진출

- **제목**: i18n (Internationalization - 다국어 처리)
- **개념설명**: 코드 내부의 '환영합니다' 한글을 지우고 변수로 대체한 뒤, 뷰포트의 언어 세팅에 따라 (ko.json, en.json) 매핑 사전을 참조시키는 기술입니다. i18n은 i와 n 사이에 글자가 18개 있다는 개발자식 축약어입니다.
- **코드예제**:

```tsx
const { t } = useTranslation();
return <button>{t('LOGIN_BTN')}</button>; // "로그인" 또는 "Login" 렌더링
```

### 57. 자바스크립트에 먹힌 스타일

- **제목**: CSS-in-JS (Styled Components)
- **개념설명**: 파일 하나에 CSS 로직을 캡슐화시킵니다. 컴포넌트 Props로 `primary={true}` 값을 전달하면 자바스크립트 엔진이 계산을 마친 후 이에 대응하는 해시태그형 가상 클래스를 동적으로 뽑아내어 CSS 충돌을 원천 박멸합니다.
- **코드예제**:

```tsx
const Box = styled.div`
  background: ${props => props.isDark ? '#333' : '#fff'};
`;
```

### 58. 마이크로 프론트엔드

- **제목**: Module Federation
- **개념설명**: 초대형 메인 웹사이트의 헤더는 A팀이, 장바구니 리스트는 B팀이, 푸터는 C팀이 별도의 레포지토리와 기술 스택으로 독립적으로 배포하되, 클라이언트 런타임에 마치 하나의 앱인 양 네트워크로 스크립트를 조립시켜 퍼즐을 맞추는 초거대 아키텍처입니다.
- **코드예제**:

```javascript
// A팀의 Webpack Config
new ModuleFederationPlugin({ remotes: { Header: 'headerApp@http://...' } })
```

### 59. 스트리밍 렌더링

- **제목**: Selective Hydration (React 18)
- **개념설명**: 옛날 React SSR은 사이트 통째로 다운을 받고 통째로 생명력을 씌웠습니다(Hydration). 개선된 스트리밍 하이드레이션은 제일 무거운 댓글창 컴포넌트는 `<Suspense>`로 묶어 로딩 서클핑빙 돌리는 동안 백그라운드로 로딩을 보내버리고, 유저가 당장 보는 뼈대만 우선적으로 생명력을 입혀 체감 성능을 박살 나게 향상시킵니다.
- **코드예제**:

```tsx
<Suspense fallback={<Spinner />}>
  <HeavyCommentsSection />
</Suspense>
```

### 60. 완벽한 상태 예측

- **제목**: 상태 기계 (XState & State Machines)
- **개념설명**: `isEditing`, `isLoading`, `isError`, `isSuccess` 보일러플레이트 부울 변수 4~10개를 사용하다 불가능한 에러 상태에 진입하는 부작용을 타파합니다. 오직 하나의 엄격한 상태 노드로만 동작하며 다른 곁가지 상태 전환을 절대로 용납하지 않는 유한 상태 기계(FSM) 아키텍처 다이어그램 기술입니다.
- **코드예제**:

```javascript
const userMachine = createMachine({
  initial: 'idle',
  states: {
    idle: { on: { SUBMIT: 'loading' } }, // 제출 누르면 로딩
    loading: { on: { SUCCESS: 'ok', ERROR: 'error' } } // 로딩 중에만 작동
  }
});
```
